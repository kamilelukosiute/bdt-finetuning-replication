# Data paths for Microviridae finetuning
# Tokenized mmap format: {prefix}_text_CharLevelTokenizer_document.{bin,idx}

train-data-paths:
  - '/workspace/bdt-finetuning-replication/data/tokenized/train_text_CharLevelTokenizer_document'
train-data-weights:
  - 1

valid-data-paths:
  - '/workspace/bdt-finetuning-replication/data/tokenized/val_text_CharLevelTokenizer_document'
valid-data-weights:
  - 1

test-data-paths:
  - '/workspace/bdt-finetuning-replication/data/tokenized/test_text_CharLevelTokenizer_document'
test-data-weights:
  - 1

checkpoint_validation_with_forward_pass: False
